# setup rvm to work
rvm default

# Set my editor to vim
set -x EDITOR vim

# setup miniconda to work
set -x PATH $PATH /Users/danielpipkin/miniconda3/bin
source (conda info --root)/etc/fish/conf.d/conda.fish
